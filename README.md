# python_opencv
this is a branch for docker python open cv env


## How to start

Docker Start

```
$ docker-compose up -d --build
```

Docker Stop

```
$ docker-compose stop
```

Docker Remove

```
$ docker-compose down
```